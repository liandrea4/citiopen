# fly.toml app configuration file generated for citiopenballkids on 2023-06-11T23:35:06-04:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "citiopenballkids"
primary_region = "iad"
kill_signal = "SIGINT"
kill_timeout = "5s"

[experimental]
  auto_rollback = true

[deploy]
  release_command = "python manage.py migrate"

[env]
  PORT = "8000"

[[services]]
  protocol = "tcp"
  internal_port = 8000
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

  [[services.ports]]
    port = 443
    handlers = ["tls", "http"]
  [services.concurrency]
    type = "connections"
    hard_limit = 25
    soft_limit = 20

  [[services.tcp_checks]]
    interval = "15s"
    timeout = "2s"
    grace_period = "1s"
    restart_limit = 0

#[[statics]]
#  guest_path = "/app/public"
#  url_prefix = "/static/"
